version: '3.4'
services:
  pix-microservices-recent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pix-microservices-recent
    ports:
      - "16093:8080"

networks:
  bubble:
    external: true